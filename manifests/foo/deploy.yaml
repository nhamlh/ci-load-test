---
apiVersion: v1
kind: Namespace
metadata:
  name: foo
---
kind: Service
apiVersion: v1
metadata:
  name: foo
spec:
  ports:
  - port: 80
    targetPort: 8080
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: foo
spec:
  template:
    spec:
      containers:
        - name: http-echo
          image: hashicorp/http-echo
          ports:
            - name: http
              containerPort: 8080
          args:
            - -listen=:8080
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: foo
spec:
  rules:
  - host: "foo.localhost"
    http:
      paths:
      - pathType: Prefix
        path: "/"
        backend:
          service:
            name: foo
            port:
              number: 80
