---
apiVersion: v1
kind: Namespace
metadata:
  name: bar
---
kind: Service
apiVersion: v1
metadata:
  name: bar
spec:
  ports:
  - port: 80
    targetPort: 8080
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: bar
spec:
  template:
    spec:
      containers:
        - name: http-echo
          image: hashicorp/http-echo
          ports:
            - name: http
              containerPort: 8080
          args:
            - -listen=:8080
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: bar
spec:
  rules:
  - host: "bar.localhost"
    http:
      paths:
      - pathType: Prefix
        path: "/"
        backend:
          service:
            name: bar
            port:
              number: 80
